overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/models/graphql/types.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
